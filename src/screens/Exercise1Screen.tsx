import { View } from "react-native";
import { Text } from "react-native-paper";
import ScreenContainer from "../components/ScreenContainer";

export default function Exercise1Screen() {
    return (
        <ScreenContainer>
            <Text> This is Excercise1!</Text>
        </ScreenContainer>
    );
}